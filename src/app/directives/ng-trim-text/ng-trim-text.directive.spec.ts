import { NgTrimTextDirective } from './ng-trim-text.directive';

describe('NgTrimTextDirective', () => {
  it('should create an instance', () => {
    const directive = new NgTrimTextDirective();
    expect(directive).toBeTruthy();
  });
});
